<div class="container">
  <div class="row">
    <div class="col-12 text-center mt-4">
      <h1><%= @diary.date.strftime('%Y-%m-%d') + "の記録" %></h1>
    </div> 
    <%= render "form", diary: @diary %>
  </div>
</div>